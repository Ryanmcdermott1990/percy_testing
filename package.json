{
  "private": true,
  "scripts": {
    "percy:cypress": "percy exec -- cypress run"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "todomvc-app-css": "^2.4.2"
  },
  "devDependencies": {
    "@percy/cli": "^1.10.0",
    "@percy/cypress": "^3.1.2",
    "cypress": "^10.7.0",
    "serve": "^14.0.1",
    "start-server-and-test": "1.14.0"
  }
}
